import 'package:flutter/material.dart';

class AssignmentsEmpty extends StatelessWidget {
  const AssignmentsEmpty({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
    //todo:
  }
}
