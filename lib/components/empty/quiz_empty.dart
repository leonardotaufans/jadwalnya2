import 'package:flutter/material.dart';

class EmptyQuiz extends StatelessWidget {
  const EmptyQuiz({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
